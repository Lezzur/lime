# ── Auth ────────────────────────────────────────────────────────────────────
# Generate with: htpasswd -bnBC 10 "" yourpassword | tr -d ':\n'
# Or: node -e "const b=require('bcryptjs'); console.log(b.hashSync('yourpassword', 10));"
AUTH_SECRET=change-me-to-a-random-32-char-string
LIME_PASSWORD_HASH=

# Optional 2FA — set to "true" to enable
LIME_2FA_ENABLED=false
# Generate with: node -e "const {authenticator}=require('otplib'); console.log(authenticator.generateSecret());"
LIME_2FA_SECRET=

# ── Backend ──────────────────────────────────────────────────────────────────
LIME_API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Push Notifications (VAPID) ───────────────────────────────────────────────
# Generate with: npm run generate-vapid
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_MAILTO=mailto:you@example.com

# ── Next.js ──────────────────────────────────────────────────────────────────
NEXTAUTH_URL=http://localhost:3000
